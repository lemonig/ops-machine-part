import { TableScrollDirective } from './table-scroll.directive';

describe('TableScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new TableScrollDirective();
    expect(directive).toBeTruthy();
  });
});
